{% extends 'base.html.twig' %}

{% block body %}

    <ol class="breadcrumb">
        <li class="breadcrumb-item"><a href="{{ path('b2b_backoffice_index') }}">Accueil</a></li>
        <li class="breadcrumb-item"><a href="{{ path('backoffice_style_index') }}">Gestion des styles et univers</a>
        </li>
        <li class="breadcrumb-item active">{{ style.label }}</li>
    </ol>

    <div class="d-flex justify-content-center">
        <a href="{{ path('backoffice_style_index') }}" class="btn btn-primary">Retour à la liste</a>
        <a href="{{ path('backoffice_style_edit', { 'id': style.id }) }}" class="btn btn-primary">Modifier</a>
    </div>

    <h1 class="text-center">Style - {{ style.label }}</h1>

    <div class="row">
        <div class="col-lg-2">
        </div>
        <div class="col-lg-8 login-form border rounded">
            <dt class="col-sm-3">Style</dt>
            <dd class="col-sm-9">{{ style.label }}</dd>
        </div>
        <div class="col-lg-2">
        </div>
    </div>

{% endblock %}
