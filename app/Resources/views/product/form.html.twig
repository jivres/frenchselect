<script type="text/javascript"
        src="https://cdnjs.cloudflare.com/ajax/libs/bootstrap-datepicker/1.4.1/js/bootstrap-datepicker.min.js"></script>
<script>
    $(document).ready(function() {
        initCollection($('ul.colors'));
        $('.js-datepicker').datepicker({
            format: 'dd/mm/yyyy'
        });
    });

    function initCollection($collectionHolder) {
        // setup an "add a tag" link
        var $addLink = $('<a href="#" class="add">Ajouter</a>');
        var $newLinkLi = $('<li></li>').append($addLink);

        // add the "add a tag" anchor and li to the tags ul
        $collectionHolder.append($newLinkLi);

        // count the current form inputs we have (e.g. 2), use that as the new
        // index when inserting a new item (e.g. 2)
        $collectionHolder.data('index', $collectionHolder.find(':input').length);

        $addLink.on('click', function(e) {
            // prevent the link from creating a "#" on the URL
            e.preventDefault();

            // add a new tag form (see next code block)
            addTagForm($collectionHolder, $newLinkLi);
        });
    }

    function addTagForm($collectionHolder, $newLinkLi) {
        // Get the data-prototype explained earlier
        var prototype = $collectionHolder.data('prototype');

        // get the new index
        var index = $collectionHolder.data('index');

        // Replace '$$name$$' in the prototype's HTML to
        // instead be a number based on how many items we have
        var newForm = prototype.replace(/__name__/g, index);

        // increase the index with one for the next item
        $collectionHolder.data('index', index + 1);

        // Display the form in the page in an li, before the "Add a tag" link li
        var $newFormLi = $('<li></li>').append(newForm);

        // also add a remove button, just for this example
        $newFormLi.append('<a href="#" class="remove">Supprimer</a>');
        $newFormLi.append('<br>------------------------------------------------------------<br>');

        $newLinkLi.before($newFormLi);

        // handle the removal, just for this example
        $('.remove').click(function(e) {
            e.preventDefault();
            $(this).parent().remove();
            return false;
        });
    }
</script>

<div class="row">

    <div class="form-group col-md-6">
        <label><span>{{ form_label(form.refModel) }}</span><span class="required-chip">*</span></label>
        {{ form_widget(form.refModel) }}
    </div>
    <div class="form-group col-md-6">
        <label><span>{{ form_label(form.name) }}</span><span class="required-chip">*</span></label>
        {{ form_widget(form.name) }}
    </div>
</div>
<div class="row">
    <div class="form-group col-md-12">
        <label><span>{{ form_label(form.label) }}</span></label>
        {{ form_widget(form.label) }}
    </div>
</div>
<div class="row">
    <div class="form-group col-md-12">
        <label><span>{{ form_label(form.description) }}</span></label>
        {{ form_widget(form.description) }}
    </div>
</div>
<div class="row">
    <div class="form-group col-md-6">
        <label><span>{{ form_label(form.country) }}</span><span class="required-chip">*</span></label>
        {{ form_widget(form.country, {'attr': {'class': 'custom-select'}}) }}
    </div>
</div>
<div class="row">
    <div class="form-group col-md-6">
        <label><span>{{ form_label(form.priceHT) }}</span><span class="required-chip">*</span></label>
        {{ form_widget(form.priceHT) }}
    </div>
    <div class="form-group col-md-6">
        <label><span>{{ form_label(form.recommendedPriceTTC) }}</span><span class="required-chip">*</span></label>
        {{ form_widget(form.recommendedPriceTTC) }}
    </div>
</div>

<div class="row">
    <div class="form-group col-md-6">
        <label><span>{{ form_label(form.material) }}</span></label>
        {{ form_widget(form.material) }}
    </div>
</div>

<div class="row">
    <div class="form-group col-md-6">
        <label><span>{{ form_label(form.mainPicture) }}</span></label>
        {{ form_widget(form.mainPicture) }}
    </div>
</div>
<div class="row">
    <div class="form-group col-md-6">
        <label><span>{{ form_label(form.maintenance) }}</span></label>
        {{ form_widget(form.maintenance) }}
    </div>
    <div class="form-group col-md-6">
        <label><span>{{ form_label(form.dimensions) }}</span></label>
        {{ form_widget(form.dimensions) }}
    </div>
</div>
<div class="row">

    <div class="form-group col-md-6">
        <label><span>{{ form_label(form.target) }}</span><span class="required-chip">*</span></label>
        {{ form_widget(form.target, {'attr': {'class': 'custom-select'}}) }}
    </div>
    <div class="form-group col-md-6">
        <label><span>{{ form_label(form.primaryCat) }}</span><span class="required-chip">*</span></label>
        {{ form_widget(form.primaryCat, {'attr': {'class': 'custom-select'}}) }}
    </div>
</div>
<div class="row">
    <div class="form-group col-md-6">
        <label><span>{{ form_label(form.secondaryCat) }}</span><span class="required-chip">*</span></label>
        {{ form_widget(form.secondaryCat, {'attr': {'class': 'custom-select'}}) }}
    </div>
    <div class="form-group col-md-6">
        <label><span>{{ form_label(form.tertiaryCategory) }}</span><span class="required-chip">*</span></label>
        {{ form_widget(form.tertiaryCategory) }}
    </div>
</div>
<div class="row">
    <div class="form-group col-md-12">
        <label><span>{{ form_label(form.sizes) }}</span></label>
        {{ form_widget(form.sizes) }}
    </div>
</div>
