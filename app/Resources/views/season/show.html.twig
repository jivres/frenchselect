{% extends 'base.html.twig' %}

{% block body %}
    <ol class="breadcrumb">
        <li class="breadcrumb-item"><a href="{{ path('b2b_backoffice_index') }}">Accueil</a></li>
        <li class="breadcrumb-item"><a href="{{ path('backoffice_season_index') }}">Gestion des saisons</a></li>
        <li class="breadcrumb-item active">{{ season.name }}</li>
    </ol>

    <div class="d-flex justify-content-center">
        <a href="{{ path('backoffice_season_index') }}" class="btn btn-primary">Retour à la liste</a>
        <a href="{{ path('backoffice_season_edit', { 'id': season.id }) }}" class="btn btn-primary">Modifier</a>
    </div>

    <h1 class="text-center">Saison - {{ season.name }}</h1>

    <div class="row">
        <div class="col-lg-2">
        </div>
        <div class="col-lg-8 login-form border rounded">
            <dt class="col-sm-3">Saison</dt>
            <dd class="col-sm-9">{{ season.name }}</dd>

            <dt class="col-sm-3">Rang</dt>
            <dd class="col-sm-9">{{ season.rank }}</dd>

        </div>
        <div class="col-lg-2">
        </div>
    </div>
{% endblock %}
