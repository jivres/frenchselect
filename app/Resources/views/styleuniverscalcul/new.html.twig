{% extends 'base.html.twig' %}

{% block body %}
    <ol class="breadcrumb">
        <li class="breadcrumb-item"><a href="{{ path('b2b_backoffice_index') }}">Accueil</a></li>
        <li class="breadcrumb-item"><a href="{{ path('backoffice_style_index') }}">Gestion des styles et univers</a>
        </li>
        <li class="breadcrumb-item active">{{ univers.label }} - Nouveau style</li>
    </ol>

    <div class="d-flex justify-content-center">
        <a href="{{ path('backoffice_univers_show', { 'id': univers.id }) }}" class="btn btn-primary">Retour à l'univers</a>
    </div>

    <h1 class="text-center">{{ univers.label }} - Nouveau style</h1>

<div class="row">
    <div class="col-lg-2">
    </div>
    <div class="col-lg-8 login-form border rounded">
    {{ form_start(form) }}
    {{ form_widget(form) }}
        <div class="text-center">
            <input class="btn btn-primary" type="submit" value="Ajouter le style"/>
        </div>
    {{ form_end(form) }}
    </div>
    <div class="col-lg-2">
    </div>
</div>

{% endblock %}
